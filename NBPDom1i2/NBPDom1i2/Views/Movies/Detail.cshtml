@model NBPDom1i2.ViewModels.MovieDetail
@{
    ViewBag.Title = "Detail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



<div class="container-fluid">
    @using (Html.BeginForm("Rent", "Movies"))
    {
    <div class="col-sm-5">
        <h2>@Html.ValueFor(m => m.movie.title)</h2>
        <br />
        <div>
            Genre: @Html.ValueFor(m => m.genre)
        </div>
        <br />
        <div>
            Description: @Model.movie.description
        </div>
        <br />
        <div>
            Release date: @Model.movie.released
        </div>
        <br />
        <div>
            Director: @Model.director
        </div>
        <br />
        <div class="list-group">
            Cast:
            <br />
            <ul class="list-group">
                @foreach (string s in Model.actors)
                {
                    <li style="margin-left: 30px">
                        @s
                    </li>
                }
            </ul>

        </div>
        <br />
        <div>
            Available copies: @Model.movie.copies
        </div>
        <br />
        @if (Model.movie.copies > 0)
        {
            <div class="form-group">
                <button type="submit" class="btn btn-primary">Rent</button>
            </div>
        }
        else
        {
        <div class="form-group">
            <button type="submit" class="btn btn-primary" disabled>Rent</button>
        </div>
        }
    </div>
    }
</div>

